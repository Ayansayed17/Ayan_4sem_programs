#include <stdio.h>
int main() {
    int n,j,i;
    scanf("%d",&n);
    if(n<1)
        printf("Invalid input.");
    else{
        for(i=1;i<=10;i++){
            for(j=1;j<=n;j++)
                printf("%dx%d=%d\t",j,i,i*j);
            printf("\n");
        }}return 0;
}
